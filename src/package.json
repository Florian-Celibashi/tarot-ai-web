{
  "name": "tarot-ai-web",
  "version": "1.0.0",
  "description": "Terminal-style creative AI tarot experience with structured deck logic",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "lint": "eslint .",
    "test": "node --test --test-reporter=spec",
    "test:watch": "node --test --watch",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "eslint": "^9.39.1",
    "supertest": "^7.1.4"
  },
  "overrides": {
    "form-data": "4.0.6",
    "path-to-regexp": "0.1.13"
  }
}
